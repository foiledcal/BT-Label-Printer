ReversePrintOrder := True
    ;Prints labels in the opposite order they're scanned
SkipFindProducts := False 
    ;If the "Find Products" window opens, does not select a SKU from that window.
    ;Prevents some wrong labels from being printed, but adds to the number of SKUs that need to be manually reviewed.
HideBisTrackonStart := False
    ;Hides BisTrack at the start of the program allowing you to use the computer for other things while it runs in the background.
DisplayBisTrackonStop := False
    ;Will pull BisTrack to the front of the desktop once all SKUs are done being entered.
    ;Otherwise, BisTrack will stay in the background without interrupting your interactions with another window.